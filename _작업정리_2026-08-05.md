# 작업 정리 — 2026-08-05

이 세션에서 진행한 전체 작업 기록. (여러 저장소에 걸쳐 있음)

---

## 1. 지역 페이지 프로그래매틱 생성 시스템 (핵심)

경쟁사(specialstudyedu 5.4만·PHP동적, powerspeak.kr 1.4만, gwaoemap 동레벨)를 벤치마크 →
**데이터셋 + 템플릿 1개 + 동적 라우팅** 구조로 세 사이트에 적용. 정의된 조합만 200, 나머지 404.

### 아키텍처
- `_local/engine.mjs` — 데이터(17도 + 시군구 + 읍면동 3,495개 `dongs.mjs`) + 렌더 + sitemap. Node import 테스트 가능.
- 라우터:
  - guide(파워잉글리쉬): `_worker.js` (Pages advanced 모드), `/sitemap-local.xml`
  - maeil·callbiz: `functions/local/[[path]].js` (Pages Functions), `/local/sitemap.xml`
- `_local/build_dongs.py` — raqoon886/Local_HangJeongDong 행정동 geojson → 로마자화·시군구 매핑 → `dongs.mjs`
- URL: `/local/{도}/{시}/{키워드}/` , `/local/{도}/{시}/{동}/{키워드}/`
- breadcrumb + 구조화데이터(Article·FAQPage·BreadcrumbList), 기존 정적 페이지 무영향

### 세 사이트 현황 (모두 키워드 60개, 각 ~5.9만 페이지 · 합계 ~17.8만)
| 사이트 | 도메인 | 키워드 | 디자인 | 배포 |
|---|---|---|---|---|
| 파워잉글리쉬 | guide.pweng.net | 영어 60 | Nova(퍼플) | git push → CF |
| 매일영 챌린지 | go.dailychallenge.co.kr | 영어 60 | **팝아트(빨강·노랑·Do Hyeon)** | `wrangler pages deploy` |
| 전국콜비즈 | callbiz.kr | 대표번호 60 | Nova(퍼플) | `wrangler pages deploy` |

- 시군구 = 60개 키워드 전부 / 읍면동 = 상위 12개(스케일 조절)
- 키워드는 CRM(keyword-crm)에서 지역결합형만 검색량 순 선별해 24→60 확장 (영어=company1, 대표번호=company4 지오테스)
- robots.txt: 3사이트 전체 허용 + AI봇 명시 + sitemap 등록(guide는 sitemap-local.xml, maeil·callbiz는 /local/sitemap.xml)

### 매일영 팝아트 디자인
- 실제 매일영 스타일로 교체: `--red:#e0362a --yellow:#f7c948 --ink:#181008`, Do Hyeon/Anton 폰트, 크림 카드 + 두꺼운 검정 테두리 + 하드 오프셋 그림자.

### 남은 것
- **구글 서치콘솔 3사이트 sitemap 제출** (색인 가속) — [[geo-indexing-workflow]]
- 네이버 서치어드바이저 등록(meta태그 + 네이버 로그인 필요). 네이버는 외부웹 불리 → 블로그·플레이스 병행 권장.
- callbiz는 git 저장소 아님(배포만 됨) — 버전관리 필요 시 git init.

---

## 2. 파워잉글리쉬 신규 사이트(pweng-web) — 사무관리 기능

레거시 사무관리 17개 중 신규에 없던 기능 구현. (저장소: pweng_renewal/pweng-web, Next.js/Prisma/Neon)

| # | 기능 | 방식 |
|---|---|---|
| 로스터 | LT Tracker 테스트예정일 로스터 모드 (/admin/level-tests) + 인쇄 | 기존 데이터 |
| 학생 트래커 | /admin/office/student-tracker (진도·상태) | 기존 데이터 |
| SCQTR | /admin/office/scqtr (수업 품질 리포트) | 기존 데이터 |
| 체크리스트 | /admin/office/checklist (업무 체크리스트) | 신규 테이블 ChecklistItem |
| 통화품질 | /admin/office/call-quality (녹음 리뷰) | 신규 테이블 CallQualityLog |
| 매니저 삭제 | /admin/managers 하드삭제 버튼 (기존은 소프트=비활성화와 동일했음) | — |

- `prisma db push`로 Neon 운영 DB에 테이블 2개(checklist_item, call_quality_log) 추가 완료.
- ⚠️ **코드는 아직 pweng-web git 미커밋/미배포** (커밋·push 시 Vercel 배포).

---

## 3. 기타

- 발행량 조정: 콜비즈 `run_daily_post.bat` 3→2, 파워잉글리쉬·매일영 CRM `geo_settings.daily_limit` 4→2 (전역 공유).
- 경쟁사 분석: specialstudyedu(영어과외 5.4만)·powerspeak(전화화상 1.4만)·gwaoemap(과외 동레벨) — 전부 데이터+템플릿+동적라우팅 방식임을 probing으로 확인.
- 관련 메모리: [[pweng-geo-local-pages]] [[maeilyeong-site-project]] [[callbiz-project]] [[keyword-crm-deploy]]
